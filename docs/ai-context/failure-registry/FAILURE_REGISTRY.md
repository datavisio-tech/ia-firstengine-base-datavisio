# Failure Registry

Known patterns must be registered here before repeat fixes become tribal knowledge.

| Pattern | Layer | Default disposition |
|---|---|---|
| spawn setup refresh | local execution | retry outside sandbox when approved |
| browser unavailable | validation | use HTTP/runtime fallback when acceptable |
