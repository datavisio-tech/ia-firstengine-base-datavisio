# Environment

DEV is local. HM validates release candidates. PROD is customer-facing and requires human approval. Secrets live outside Git and must be injected through local env files or GitHub Environments.
